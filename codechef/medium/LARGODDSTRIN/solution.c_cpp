void findLargestOddSubstring(string num) {
    int size = num.length();
    for(int i = size - 1; i >= 0; i--){
        if((num[i]-'0') % 2 != 0){
            cout<<num.substr(0,i+1);
            return;
        }
        
    }
    
    cout<<-1;
    
}